# Local assistant / editor state
.DS_Store
Thumbs.db

# Secrets and local config
.env
.env.local
.env.*.local
config.local.yaml
config.*.local.yaml
secrets/

# Project runtime config and local state
config.yaml
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Go build outputs
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
bin/
dist/
build/
coverage.out

# Logs
*.log
logs/

# IDEs
.idea/
.vscode/

# Claude Code local state (agents, worktrees, local settings)
.claude/agents/
.claude/worktrees/
.claude/settings.local.json

# Test / QA artifacts (screenshots, Playwright MCP runtime, data dumps)
*.png
screenshots/
.playwright-mcp/
matches-*.yml

# Build artifacts (bare Go binary named `server`, SQL dumps, archive dirs)
/server
db/
**/*.sql.dump

# Stale scratch docs
BLOCKED-*.md
plan.md
xolto-hardening.md
